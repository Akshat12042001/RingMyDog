const {StyleSheet} = require('react-native');

const styles = StyleSheet.create({
  itemSubContainer: {
    flexDirection: 'row',
    flex: 1,
  },
  text: {
    fontWeight: 'bold',
  },
});
export default styles;
